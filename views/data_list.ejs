<!DOCTYPE html>
<html>
    <head>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <script>
            function board(){
                location.href = "/main/board"
            }
            function info(no){
                location.href  = "/main/info?no="+no
            }
            function add_img(){
                location.href = '/main/add_img'
            }
            function back(){
                location.href = "/main"
            }
            function logout(){
                location.href = "/logout"
            }
        </script>
        <%- include("./head.ejs")%>
    </head>
    <body>
        <%- include("./nav.ejs")%>
        <div class="container" style="margin-top: 60px;">
            <div align="center" style="margin-bottom: 30px;">
                <h1><b>게시판</b></h1>
            </div>
            <table class="table table-striped table-hover">
            <tr style="width: 15px;">
                <th>No</th>
                <th>Title</th>
                <th>IMG</th>
                <th>Author</th>

            </tr>
           
            <%for(var i=0; i < content.length; i++){%>
            <tr onclick="info('<%=content[i].No%>')">
                <td><%=content[i].No%></td>
                <td><%=content[i].title%></td>
                <td><img src = '<%=content[i].img%>' width="100"></td>
                <td><%=content[i].post_id%></td>
            </tr>
            <%}%>
        </table>
            <div align="right" style="margin-bottom: 30px">
                <button class="btn btn-primary" onclick="board()">글쓰기</button>
        
                <button class="btn btn-secondary" onclick="back()">뒤로가기</button>
            </div>
        
        </div>
        
        
    </body>
</html>
